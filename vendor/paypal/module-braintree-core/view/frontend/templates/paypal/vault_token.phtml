<?php
// @codingStandardsIgnoreFile

/** @var \PayPal\Braintree\Block\Customer\PayPal\VaultTokenRenderer $block */
?>
<tr>
    <td data-th="<?= $block->escapeHtmlAttr(__('PayPal Account')) ?>" class="col paypal-account">
        <img src="<?= $block->escapeHtmlAttr($block->getIconUrl()) ?>"
             width="<?= $block->escapeHtmlAttr($block->getIconWidth()) ?>"
             height="<?= $block->escapeHtmlAttr($block->getIconHeight()) ?>"
             alt="<?= $block->escapeHtmlAttr(__('PayPal Logo')) ?>">
        <span><?= $block->escapeHtml($block->getPayerEmail()) ?></span>
    </td>
    <td data-th="<?= $block->escapeHtmlAttr(__('Actions')) ?>" class="col actions">
        <form class="form"
              action="<?= $block->escapeUrl($block->getUrl('vault/cards/deleteaction')) ?>"
              method="post">
            <?= $block->getBlockHtml('formkey') ?>
            <input name="public_hash"
                   value="<?= $block->escapeHtmlAttr($block->getToken()->getPublicHash()) ?>"
                   type="hidden"/>
            <button type="submit"
                    class="action delete"
                    data-mage-init='{
                        "Magento_Vault/js/customer_account/deleteWidget":{
                            "type": "popup",
                            "modalClass": "my-credit-cards-popup",
                            "toggleEvent": "click",
                            "title": "<?= $block->escapeHtmlAttr(__('Delete')) ?>",
                            "content": "Are you sure you want to delete this PayPal account: <?= $block->escapeHtmlAttr($block->getPayerEmail()) ?>?"
                        }
                    }'>
                <span><?= $block->escapeHtml(__('Delete')) ?></span>
            </button>
        </form>
    </td>
</tr>
